namespace ChessGame
{
    public partial class Form1 : Form
    {

        public Form1()
        {
            InitializeComponent();
            displayBoard();
            // displayPieces();
        }

        /*public void displayPieces(List<Piece> pieces)
        {
            //foreach () Image = LoadPieceImage(gameBoard.matrix[rank, file]._pieceOnSquare, rank < 2 ? 'w' : 'b'),

        }*/

    }
}